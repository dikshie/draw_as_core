name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  lint:
    name: Lint & Format Check (Ruff)
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
          cache-dependency-glob: |
            uv.lock
            pyproject.toml

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dev dependencies
        run: uv sync --extra dev

      - name: Run Ruff Linter
        run: uv run ruff check .

      - name: Run Ruff Formatter Check
        run: uv run ruff format --check .

  test:
    name: Test & Smoke Build (Python ${{ matrix.python-version }})
    needs: lint
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
          cache-dependency-glob: |
            uv.lock
            pyproject.toml

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --python ${{ matrix.python-version }} --extra dev

      - name: Run Pytest Suite (IPv4 & IPv6)
        run: uv run --python ${{ matrix.python-version }} pytest -v test_as_core.py test_as_core_IPv6.py

      - name: Smoke Test - Generate Global IPv4 & IPv6 Topology
        run: |
          uv run --python ${{ matrix.python-version }} python as_core.py -c GLOBAL -o as_core_smoke.png
          uv run --python ${{ matrix.python-version }} python as_core_IPv6.py -c GLOBAL -o as_core_ipv6_smoke.png
          uv run --python ${{ matrix.python-version }} python as_core.py -c ID -o as_core_id_smoke.png
          uv run --python ${{ matrix.python-version }} python as_core_IPv6.py -c ID -o as_core_ipv6_id_smoke.png

      - name: Upload Smoke Test Artifacts
        if: matrix.python-version == '3.12'
        uses: actions/upload-artifact@v7
        with:
          name: sample-visualizations
          path: "*smoke.png"
          retention-days: 7
